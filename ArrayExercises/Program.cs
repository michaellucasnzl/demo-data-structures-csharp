/*
 * =============================================================================
 * ARRAY EXERCISES - C# Data Structures
 * =============================================================================
 * 
 * Arrays are fixed-size, zero-indexed collections of elements of the same type.
 * They provide O(1) access by index but O(n) for insertion/deletion.
 * 
 * =============================================================================
 * BASIC OPERATIONS TO PRACTICE:
 * =============================================================================
 * 
 * 1. Declaration & Initialization
 *    - Declare an array with a specific size
 *    - Initialize an array with values
 *    - Create a multi-dimensional array (2D, jagged)
 * 
 * 2. Accessing Elements
 *    - Access element by index
 *    - Modify element at index
 *    - Get array length
 * 
 * 3. Iteration
 *    - Loop through array using for loop
 *    - Loop through array using foreach
 *    - Use Array.ForEach() method
 * 
 * 4. Common Array Methods
 *    - Array.Sort() - Sort elements
 *    - Array.Reverse() - Reverse order
 *    - Array.IndexOf() - Find index of element
 *    - Array.Find() - Find element matching predicate
 *    - Array.FindAll() - Find all matching elements
 *    - Array.Exists() - Check if element exists
 *    - Array.Copy() - Copy elements to another array
 *    - Array.Clear() - Reset elements to default
 *    - Array.Resize() - Resize the array
 * 
 * 5. LINQ with Arrays
 *    - Where, Select, OrderBy, Aggregate, etc.
 * 
 * =============================================================================
 * EXERCISES:
 * =============================================================================
 * 
 * Exercise 1: Two Sum
 *    Given an array of integers and a target sum, find two numbers that add up
 *    to the target. Return their indices.
 *    Example: [2, 7, 11, 15], target = 9 → [0, 1]
 * 
 * Exercise 2: Find Maximum and Minimum
 *    Find the largest and smallest elements in an array without using
 *    built-in Min/Max methods.
 * 
 * Exercise 3: Reverse an Array In-Place
 *    Reverse the array without creating a new array.
 * 
 * Exercise 4: Remove Duplicates from Sorted Array
 *    Given a sorted array, remove duplicates in-place and return the new length.
 * 
 * Exercise 5: Rotate Array
 *    Rotate an array to the right by k steps.
 *    Example: [1,2,3,4,5,6,7], k=3 → [5,6,7,1,2,3,4]
 * 
 * Exercise 6: Find Missing Number
 *    Given an array containing n distinct numbers from 0 to n, find the missing one.
 *    Example: [3, 0, 1] → 2
 * 
 * Exercise 7: Move Zeros
 *    Move all zeros to the end while maintaining order of non-zero elements.
 *    Example: [0,1,0,3,12] → [1,3,12,0,0]
 * 
 * Exercise 8: Merge Two Sorted Arrays
 *    Merge two sorted arrays into one sorted array.
 * 
 * Exercise 9: Find Duplicate
 *    Find the duplicate number in an array of n+1 integers where each integer
 *    is between 1 and n inclusive.
 * 
 * Exercise 10: Maximum Subarray (Kadane's Algorithm)
 *    Find the contiguous subarray with the largest sum.
 *    Example: [-2,1,-3,4,-1,2,1,-5,4] → 6 (subarray [4,-1,2,1])
 * 
 * =============================================================================
 */

Console.WriteLine("=== Array Exercises ===");
Console.WriteLine("Complete the exercises above to master C# Arrays!");
Console.WriteLine();

// Your code here...
